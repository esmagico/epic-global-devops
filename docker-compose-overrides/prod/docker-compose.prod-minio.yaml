include:
  - ./common/minio/docker-compose.yaml

services:
  caddy:
    extends:
      file: ./common/caddy/docker-compose.yaml
      service: caddy
    volumes:
      - ./caddyfile-overrides/prod/Caddyfile.prod-minio:/etc/caddy/Caddyfile
volumes:
  caddy_data:
  caddy_config: