input {
  beats {
    port => 5044
  }
}

filter {
  # Example: Add processing logic here
  mutate {
    add_field => { "processed" => "true" }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "filebeat-logs-%{+YYYY.MM.dd}"
  }

  # Optional: Debug logs in the console
  stdout {
    codec => rubydebug
  }
}
